<%= form_for @tick do |f|%>


    <%#                      should match label, collection, what you want, what they see %>
    <%= f.label :climb_id, "Climb" %> <br>
    <%= f.collection_select :climb_id, Climb.all, :id, :name, prompt: true %><br>
    <%= f.hidden_field :climber_id, value: @current_climber.id %><br>


    <%= f.submit %>
<% end %>
